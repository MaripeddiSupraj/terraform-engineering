# Azure Key Vault

Secure baseline Key Vault using Azure RBAC authorization, soft delete, and purge protection. Public network access defaults to disabled; callers that need a usable private vault must compose private endpoint/DNS connectivity.
