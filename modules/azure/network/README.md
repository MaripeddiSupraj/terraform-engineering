# Azure Network

Creates one virtual network and a map of subnets. The caller owns topology decisions (hub/spoke, route tables, private endpoints, firewalls); this module keeps the primitive network interface small and composable.
