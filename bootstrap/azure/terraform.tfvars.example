resource_group_name           = "rg-tfstate-platform-ci"
location                      = "centralindia"
storage_account_name          = "sttfstateCHANGE001"
container_name                = "tfstate"
public_network_access_enabled = true

# Add only the egress IP/CIDR of the trusted bootstrap runner when public
# network access is required. Prefer private connectivity for steady state.
allowed_ip_rules = ["203.0.113.10"] # replace with your runner egress IP

# Identities that run plan/apply (for example a GitHub OIDC service principal).
state_writer_principals = {
  # github-plan = { principal_id = "00000000-0000-0000-0000-000000000000" }
}

tags = {
  managed-by  = "terraform"
  purpose     = "terraform-state"
  environment = "shared"
}
